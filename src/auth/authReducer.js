import * as types from "./actionTypes";

const initialState = {
  user: null
};

export const authReducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};
